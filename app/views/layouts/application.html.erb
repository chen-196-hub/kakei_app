<!DOCTYPE html>
<html>
  <head>
    <title>TheKakeiApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="header-logo">
        <!-- <% if @current_user %>
          <%= link_to("TweetApp", "/user/show") %>
        <% else %>
          <%= link_to("TweetApp", "/") %>
        <% end %> -->
        <%= link_to("KakeiNet", root_path) %>
      </div>
      <ul class="header-menus">
        <li>
          ログインしてるID<%= session[:user_id]%>
        </li>
        <li>
          <%= link_to("KakeiNetとは？", "/about") %>
        </li>
        <li>
         <%= link_to("メンバー一覧", "/users/index") %>
        </li>
        <li>
          <%= link_to("新規登録", "/users/new") %>
        </li>
        <li>
         <%= link_to(" ログイン", "/login") %>
        </li>
        <li>
         <%= link_to(" ログアウト", "/logout") %>
        </li>
        <li>
          <%= link_to("TODO", "/tasks/index") %>
        </li>
        <li>
          <%= link_to("家計", "/household/index") %>
        </li>
      </ul>
    </header>
    <% if flash[:notice]%>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
